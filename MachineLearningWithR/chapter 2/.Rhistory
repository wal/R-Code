range(usedcars$price)
summary(usedcars$price)
hist(usedcars$price)
summary(usedcars$mileage)
boxplot(usedcars$mileage)
install.packages("gmodels")
?CrossTable
usedcars$model
usedcars$conservative
hist(usedcars$price)
history;
history()
history(2)
install.packages("gmodels")
usedcars
str(usedcars)
CrossTable(data=usedcars, model, transmission)
CrossTable(usedcars$model, usedcars$transmission)
CrossTable(usedcars$model, usedcars$transmission, usedcars$conservative)
CrossTable(usedcars$model, usedcars$transmission)
CrossTable(usedcars$model, usedcars$mpg)
CrossTable(usedcars$model, usedcars$mpg)
str(usedcars)
CrossTable(usedcars$model, usedcars$mileage)
seq(from = 0, to = 1, by = 0.2)
xtabs(usedcars$model, usedcars$conservative)
xtabs(usedcars$model ~ usedcars$conservative)
xtabs(data=usedcars, ~ model + conservative + year)
xtabs(data=usedcars, ~ model + conservative)
ftable(xtabs(data=usedcars, ~ model + conservative))
ftable(xtabs(data=usedcars, ~ model + conservative + year))
ftable(xtabs(data=usedcars, ~ model + conservative + year))
usedcars
head(usedcars)
exit
data <- read.csv('usedcars.csv', stringsAsFactors=TRUE)
str(data)
library(gmodels)
CrossTable(data$color, data$year)
str(data)
CrossTable(data$model, data$transmission)
setwd("~/projects/wal/MachineLearningWithR/chapter 3/")
setwd("~/projects/wal/MachineLearningWithR/chapter 4/")
install.packages("tm")
