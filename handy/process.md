# Understand the business problem
# Define the objective
# How was the data collected ? - process ?

# Initialise tidyverse library
# Load in data
# Is there missing data ? - identify the columns with missing data
# Any hunches ?

# Find the correlation between numerical variables and target variable 

# Examine the outut variable ? 
